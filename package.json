{
  "name": "sistran-tests-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for SISTRAN technical tests",
  "scripts": {
    "dev": "pnpm --parallel run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  }
}
